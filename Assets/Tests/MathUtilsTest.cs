using NUnit.Framework;

namespace Tests {
    public class MathUtilsTest {
        // A Test behaves as an ordinary method
        [Test]
        public void MathUtilsTestSimplePasses() {
            //var direction = Vector2.left.Rotate(60f);
            //var max = new Vector2(-2, -2);
            //var grownDirection = MathUtils.GrowDirectionToMax(direction, max);

            //Assert.AreEqual(direction.normalized, grownDirection.normalized);
        }
    }
}